<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Edu+AU+VIC+WA+NT+Dots:wght@400..700&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Rubik+Wet+Paint&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Login Form</title>
    <style>
        body {
            background:linear-gradient(to  bottom right, rgb(0, 0, 0), rgb(87, 86, 86));
            background-repeat: no-repeat;
            background-size: cover;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            width: 300px;
            padding: 30px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            backdrop-filter: blur(15px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }
        h1 {
            text-align: center;
            color: #ffffff;
            margin-bottom: 20px;
        }
        .input-group {
            position: relative;
            margin: 10px 0;
         
        }
        input[type="email"],
        input[type="password"] {
            width: 75%;
            padding: 12px 40px; 
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.3);
            color: #5a5a5a;
            font-size: 16px;
            outline: none;
            backdrop-filter: blur(15px);
            box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.2);
            transition: background-color 0.3s;
           
        }
        input::placeholder {
            color: #474747;
            opacity: 0.8; 
        }
        input:hover {
            background: rgba(255, 255, 255, 0.5);
        }
        .icon {
            position: absolute;
            left: 10px;
            top: 50%;
            transform: translateY(-50%);
            color: #000000;
        }
        button {
            width: 49%;
            padding: 12px;
            border: none;
            border-radius: 5px;
            background: #8a2be2;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
            transition: all 0.3s ease;
            margin-top: 10px;
        }
        button:hover {
            background-color: #a964e9;
        }
        button {
            font-size: 18px;
        }
        .button-glow {
      animation: glow 1.5s ease-in-out infinite;
    }
        @keyframes glow {
      0%, 100% {
        box-shadow: 0px 0px 10px rgba(184, 128, 243, 0.8);
      }
      
    }
        a {
            color:rgba(252, 252, 252, 0.8);
            text-decoration: none;
            margin-left: 10px;
            font-size: 17px;
        }
        img {
            width: 60px;
            height: 60px;
        }
        h3 {
            margin-bottom: 29px;
            font-family: "Montserrat", sans-serif;
  font-optical-sizing: auto;
  font-weight: 500;
  font-style: normal;
  text-align:center ;
        }
      #avatar{
            font-size: 60px;
            color: #000000;
            margin-bottom: 10px;
      }


    </style> <style>
        /* Global styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: white; /* Set the background of the body to white */
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            margin: 0;
        }

        /* Navigation bar with gradient */
        nav {
            width: 100%;
            padding: 15px 30px;
            background: linear-gradient(to right, #ff4081, #7e57c2); /* Gradient navbar */
            position: fixed;
            top: 0;
            left: 0;
            z-index: 10;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }

        nav h1 {
            font-size: 1.8rem;
            letter-spacing: 2px;
            font-weight: bold;
        }

        nav ul {
            list-style-type: none;
            display: flex;
            gap: 20px;
        }

        nav ul li {
            font-size: 1.1rem;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: #ff4081;
        }

        /* Login Form Container */
        .container {
            width: 100%;
            max-width: 400px;
            padding: 40px;
            background: rgba(255, 255, 255, 0.9); /* Slightly opaque white background */
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin-top: 100px; /* Space for the navbar */
        }

        h3 {
            text-align: center;
            color: #333;
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .input-group {
            position: relative;
            margin: 15px 0;
        }

        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 15px;
            border: 2px solid #ddd;
            border-radius: 8px;
            background: #f9f9f9;
            color: #333;
            font-size: 16px;
            outline: none;
            transition: 0.3s ease-in-out;
        }

        input::placeholder {
            color: #aaa;
            opacity: 0.7;
        }

        input:focus {
            border-color: #7e57c2;
            background-color: #fff;
        }

        .icon {
            position: absolute;
            left: 10px;
            top: 50%;
            transform: translateY(-50%);
            color: #7e57c2;
        }

        button {
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 8px;
            background-color: #7e57c2;
            color: white;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-top: 15px;
        }

        button:hover {
            background-color: #6d47a4;
        }

        a {
            color: #7e57c2;
            text-decoration: none;
            font-size: 14px;
            display: block;
            text-align: center;
            margin-top: 15px;
        }

        a:hover {
            color: #ff4081;
        }

        #avatar {
            font-size: 70px;
            color: #7e57c2;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Footer text */
        h3 {
            text-align: center;
            font-family: "Montserrat", sans-serif;
            font-weight: 500;
            margin-top: 30px;
            color: #7e57c2;
        }
    </style>
</head>
<body>
    <div class="container button-glow">
        <form action="/auth/login" method="POST">
        <center><i class="fas fa-user user-icon" id="avatar"></i></center>
        <h3>Login Here</h3>
        <div class="input-group">
            <i class="fas fa-envelope icon"></i>
            <input type="email" name="email" placeholder=" Your email..." >
        </div>
        <div class="input-group">
            <i class="fas fa-lock icon"></i>
            <input type="password" name="password"placeholder="Password"  >
        </div>
        <button type="submit" >Login</button>
        <a href="../register">Create Account</a>
    </form>
    </div>
</body>
</html>
